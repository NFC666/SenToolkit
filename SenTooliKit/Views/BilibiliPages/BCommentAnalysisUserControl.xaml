<UserControl x:Class="SenTooliKit.Views.BilibiliPages.BCommentAnalysisUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SenTooliKit.Views.BilibiliPages"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:SenTooliKit.ViewModels.BilibiliPages"
             d:DataContext="{d:DesignInstance vm:BCommentAnalysisUserControlVM, IsDesignTimeCreatable=True}"
             d:DesignHeight="800" d:DesignWidth="1200">
  <Grid Margin="10">

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="80*" />
        <ColumnDefinition Width="20*" />
      </Grid.ColumnDefinitions>
      <TextBox
        Text="{Binding Url}" />
      <Button Grid.Column="1"
              Content="分析"
              Command="{Binding GetCommentDetailInfoCommand}" />
    </Grid>
    <ProgressBar Grid.Row="0"
                 IsIndeterminate="{Binding IsLoading}"
                 Background="Transparent"
                 BorderBrush="Transparent"
                 VerticalAlignment="Bottom"/>
    <Grid Grid.Row="1" VerticalAlignment="Top">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="50*" />
        <ColumnDefinition Width="50*" />
      </Grid.ColumnDefinitions>
      <GroupBox
        Margin="10"
        Header="词频分析饼状图"
        Height="500">
        <ContentControl IsEnabled="False" 
                        Content="{Binding PiePlot}"/>
      </GroupBox>
    </Grid>
  </Grid>
</UserControl>